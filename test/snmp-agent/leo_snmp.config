[{snmp, [{agent, [{versions, [v1, v2]},
                  {db_dir, "test/snmp-agent/db"},
                  {config, [{dir, "test/snmp-agent/agent/conf"},
                  {verbosity, silence}]},
                  {net_if, [{verbosity, silence}]}]},
         {verbosity, silence}]
}].
